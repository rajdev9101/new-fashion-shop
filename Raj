<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>New Fashion Shop</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f4f4f4; margin: 0; padding: 0; }
    header { background: #222; color: #fff; padding: 20px; display: flex; justify-content: space-between; align-items: center; }
    header h1 { margin: 0; }
    header .dev { font-size: 14px; color: #ffcc00; }
    .gallery { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 15px; padding: 20px; }
    .card { background: #fff; padding: 10px; border-radius: 10px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
    .card img { width: 100%; border-radius: 10px; }
    .desc { margin: 10px 0; }
    .actions { display: flex; justify-content: space-between; align-items: center; }
    .like { cursor: pointer; font-size: 20px; color: gray; }
    .like.liked { color: red; }
    .comments { margin-top: 10px; }
    .comments p { background: #f1f1f1; padding: 5px; border-radius: 5px; position: relative; }
    .delete { position: absolute; right: 5px; top: 5px; cursor: pointer; color: red; font-size: 12px; }
    footer { background: #222; color: #fff; padding: 20px; text-align: center; margin-top: 30px; }
    .upload-section { padding: 20px; background: #fff; margin: 20px; border-radius: 10px; }
  </style>
</head>
<body>

<header>
  <h1>👗 New Fashion Shop</h1>
  <div class="dev">Developer: Rajdev</div>
</header>

<div class="upload-section" id="owner-upload">
  <h2>📤 Upload New Cloth Photo</h2>
  <input type="file" id="photoInput" accept="image/*">
  <input type="text" id="descInput" placeholder="Enter cloth description">
  <input type="text" id="mrpInput" placeholder="Enter MRP">
  <button onclick="uploadPhoto()">Upload</button>
</div>

<div class="gallery" id="gallery"></div>

<footer>
  <p>🎬 Agar aap log FREE mein movie dekhna chahte ho to <a href="https://t.me/yourMovieChannel" target="_blank">Mera Telegram Channel</a> jarur join karein.</p>
  <p>Agar website banwana hai to Telegram pe contact karein 📲</p>
  <p><b>About:</b> Ek passionate developer jo naye designs aur technology se unique websites banata hai. Ye Fashion Shop unhi creations ka ek udaharan hai. ✨</p>
</footer>

<script>
let gallery = document.getElementById("gallery");

function uploadPhoto() {
  let file = document.getElementById("photoInput").files[0];
  let desc = document.getElementById("descInput").value;
  let mrp = document.getElementById("mrpInput").value;

  if (!file) { alert("Please select a photo!"); return; }

  let reader = new FileReader();
  reader.onload = function(e) {
    let card = document.createElement("div");
    card.className = "card";
    card.innerHTML = `
      <img src="${e.target.result}" alt="Cloth">
      <div class="desc">${desc || "No description"}</div>
      <p><b>MRP:</b> ₹${mrp || "N/A"}</p>
      <div class="actions">
        <span class="like" onclick="toggleLike(this)">❤️</span>
        <button onclick="contact()">Contact</button>
      </div>
      <div class="comments">
        <input type="text" placeholder="Write a comment..." onkeydown="postComment(event,this)">
      </div>
    `;
    gallery.prepend(card);
  };
  reader.readAsDataURL(file);
}

function toggleLike(el) {
  el.classList.toggle("liked");
}

function contact() {
  alert("Contact us via WhatsApp: +91XXXXXXXXXX or Instagram: @yourfashionid");
}

function postComment(event, input) {
  if (event.key === "Enter" && input.value.trim() !== "") {
    let p = document.createElement("p");
    p.textContent = input.value;
    let del = document.createElement("span");
    del.textContent = "❌";
    del.className = "delete";
    del.onclick = function(){ p.remove(); }
    p.appendChild(del);
    input.parentElement.appendChild(p);
    input.value = "";
  }
}
</script>

</body>
</html>
